import React, { useState, useEffect, useRef } from "react";

import {
    Container,
    InputGroup,
    FormControl,
    Form,
    Row,
    Col,
    Button,
    Badge,
} from "react-bootstrap";
import "bootstrap/dist/css/bootstrap.min.css";
import { Redirect } from "react-router";

const Main = () => {
    return <Redirect to="/class"></Redirect>;
};

export default Main;
