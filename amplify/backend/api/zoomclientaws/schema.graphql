type Traffic @model {
  id: ID!
  studentId: String!
  affiliation: String!
  meetingId: String!
  hash: String!
  pageNumber: Int!
  state: String!
  dateTime: AWSDateTime!
}

type File @model {
  id: ID!
  name: String!
  hash: String!
  uploadedTime: AWSDateTime!
}

type Page @model {
  id: ID!
  hash: String!
  numPages: Int!
  pageNumber: Int!
  finishedTime: AWSDateTime!
}