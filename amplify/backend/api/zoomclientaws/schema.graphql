type Traffic @model {
  id: ID!
  studentId: String!
  meetingId: String!
  state: String!
  dateTime: AWSDateTime!
}

type File @model {
  id: ID!
  name: String!
  hash: String!
}

type Page @model {
  id: ID!
  hash: String!
  numPages: Int!
  pageNumber: Int!
  finishedTime: AWSDateTime!
}